/*1) Escreva um algoritmo para calcular o consumo médio de um automóvel 
(medido em km/l), dado que são conhecidos a distância total percorrida 
e o volume de combustível consumido para percorrê-la (medido em litros).*/

//Insira a kilometragem percorrida: 
let km = 1000;

// Insira a quantidade de litros:
let litros = 55;

var soma = (km/litros);

console.log("O Consumo de combústivel é de: " + soma + " KM/L.");